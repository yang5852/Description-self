think about use the different OPs to control  multi CA
1.	对于OP而言,无论当前显示的是何画面,其状态指示灯所显示的一直为最低级(即OP所选的全部可控区域的状态集合).
2.	OP中模式切换使用使能钥匙,触摸屏勾选功能组,切换实体按钮.未选中任何功能组时,不能操作.勾选大于等于2个组时,实体状态指示灯显示的是最低级.
3.	触摸屏下载时,首先选择该HMI的OP号用来做地址定位.之后在设置画面选择可显示的操作组,用来指定DB中OP下的操作组Display位,没有选中显示的画面,再使用使能钥匙操作时,画面不可计划.
	
